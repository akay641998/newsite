name: Build & Push Docker Image

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

env:
  IMAGE_NAME: acmecorp-website

jobs:
  build:
    name: Build Docker Image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        run: |
          docker build -t $IMAGE_NAME:${{ github.sha }} .
          docker tag  $IMAGE_NAME:${{ github.sha }} $IMAGE_NAME:latest
          echo "Image built successfully: $IMAGE_NAME:${{ github.sha }}"

      # --------------------------------------------------------
      # Optional: push to a container registry
      # Uncomment and configure one of the sections below.
      # --------------------------------------------------------

      # --- Docker Hub ---
      # - name: Log in to Docker Hub
      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ secrets.DOCKERHUB_USERNAME }}
      #     password: ${{ secrets.DOCKERHUB_TOKEN }}
      #
      # - name: Push to Docker Hub
      #   run: |
      #     docker tag $IMAGE_NAME:latest ${{ secrets.DOCKERHUB_USERNAME }}/$IMAGE_NAME:latest
      #     docker push ${{ secrets.DOCKERHUB_USERNAME }}/$IMAGE_NAME:latest

      # --- OpenShift Internal Registry / Quay.io ---
      # - name: Log in to Quay.io
      #   uses: docker/login-action@v3
      #   with:
      #     registry: quay.io
      #     username: ${{ secrets.QUAY_USERNAME }}
      #     password: ${{ secrets.QUAY_TOKEN }}
      #
      # - name: Push to Quay.io
      #   run: |
      #     docker tag $IMAGE_NAME:latest quay.io/${{ secrets.QUAY_USERNAME }}/$IMAGE_NAME:latest
      #     docker push quay.io/${{ secrets.QUAY_USERNAME }}/$IMAGE_NAME:latest
